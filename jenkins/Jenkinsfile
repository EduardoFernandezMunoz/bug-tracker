pipeline {
    agent any

    tools {
        go 'Go'                // Nombre exacto de Go en Jenkins Global Tool Config
        nodejs 'NodeJS_LTS'    // Nombre exacto de NodeJS en Jenkins Global Tool Config
    }

    stages {

        stage('Execute Unit Tests') {
            parallel {

                stage('Unit Tests - Backend') {
                    steps {
                        dir('bugtracker-backend') {
                            bat 'go version'

                            // Instala go-junit-report para convertir tests a JUnit
                            bat 'go install github.com/jstemmer/go-junit-report@latest'

                            // Ejecuta tests y genera cobertura
                            bat 'go test -v ./... > reports/test-results.txt'
                            bat 'go test -coverprofile=coverage.out -covermode=atomic ./...'
                            bat 'go tool cover -html=coverage.out -o reports/coverage.html'

                            // Convierte test-results.txt a XML para Jenkins
                            bat '%USERPROFILE%\\go\\bin\\go-junit-report < reports/test-results.txt > reports/backend-test-results.xml'
                        }
                    }
                    post {
                        always {
                            junit 'bugtracker-backend/reports/backend-test-results.xml'
                            publishHTML([ 
                                reportDir: 'bugtracker-backend/reports',
                                reportFiles: 'coverage.html',
                                reportName: 'Backend Coverage Report',
                                keepAll: true,
                                allowMissing: true,
                                alwaysLinkToLastBuild: true
                            ])
                        }
                    }
                }

                stage('Unit Tests - Frontend') {
                    steps {
                        dir('bugtracker-frontend') {
                            bat 'node -v'
                            bat 'npm -v'

                            // Instala dependencias y ejecuta tests
                            bat 'npm ci'
                            bat 'npm test -- --reporters=default --reporters=jest-junit'
                        }
                    }
                    post {
                        always {
                            junit 'bugtracker-frontend/reports/junit.xml'
                            publishHTML([ 
                                reportDir: 'bugtracker-frontend/reports/coverage',
                                reportFiles: 'index.html',
                                reportName: 'Frontend Coverage Report',
                                keepAll: true,
                                allowMissing: true,
                                alwaysLinkToLastBuild: true
                            ])
                        }
                    }
                }

            } // end parallel
        }

        stage('Launch Applications') {
            steps {
                echo "Launching backend and frontend apps with Docker..."
                script {
                    if (fileExists('docker-compose.yml')) {
                        bat 'docker-compose up --build -d'
                    } else {
                        echo "No docker-compose.yml found, skipping launch"
                    }
                }
            }
        }

        stage('API Tests') {
            agent {
                docker {
                    image 'mcr.microsoft.com/playwright:v1.50.0-jammy'
                    reuseNode true
                    args '-u 0 --network=host'
                }
            }
            steps {
                dir('tests-api') {
                    bat 'npx wait-port http://localhost:8080/api/health -t 30000'
                    bat 'npm ci'
                    bat 'npx playwright test'
                }
            }
            post {
                always {
                    junit 'tests-api/test-results/results.xml'
                    publishHTML target: [
                        reportDir: 'tests-api/playwright-report',
                        reportFiles: 'index.html',
                        reportName: 'Playwright API Test Report',
                        allowMissing: true
                    ]
                }
            }
        }

        stage('E2E Tests') {
            agent {
                docker {
                    image 'mcr.microsoft.com/playwright:v1.50.0-jammy'
                    reuseNode true
                    args '-u 0 --network=host'
                }
            }
            steps {
                dir('tests-e2e') {
                    bat 'npx wait-port http://localhost:3000 -t 30000'
                    bat 'npm ci'
                    bat 'npx playwright test'
                }
            }
            post {
                always {
                    junit 'tests-e2e/test-results/results.xml'
                    publishHTML target: [
                        reportDir: 'tests-e2e/playwright-report',
                        reportFiles: 'index.html',
                        reportName: 'Playwright E2E Test Report',
                        allowMissing: true
                    ]
                }
            }
        }
                stage('Performance Tests') {
            agent {
                docker {
                    image 'grafana/k6:latest'
                    reuseNode true
                    args '--network=host -u root --entrypoint=""'
                }
            }
            steps {
                dir('tests-perf') {
                    // En Windows con Docker, usamos sh dentro del contenedor Linux
                    bat 'docker run --rm -v "%cd%/tests-perf:/tests" -w /tests grafana/k6:latest run script.js'
                }
            }
        }
    } // end stages

    post {
        always {
            echo "Cleaning up workspace and Docker containers..."
            cleanWs()
            script {
                if (fileExists('docker-compose.yml')) {
                    bat 'docker-compose down'
                } else {
                    echo "No docker-compose.yml found, skipping cleanup"
                }
            }
        }
    }
}
