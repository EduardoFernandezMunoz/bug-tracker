pipeline {
    agent any

    stages {
        stage('Execute Unit Tests') {
            parallel {

                stage('Unit Tests - Backend') {
                    steps {
                        dir('bugtracker-backend') {
                            // Ejecuta tests de Go en Windows
                            bat 'go test -v ./... > test-results.txt'
                            bat 'go test -coverprofile=coverage.out -covermode=atomic ./...'
                            bat 'go tool cover -html=coverage.out -o coverage.html'

                            // Crear carpeta de reportes si no existe
                            bat 'if not exist reports mkdir reports'
                            bat 'move /Y coverage.html reports\\'
                        }
                    }
                    post {
                        always {
                            // Publicar reporte HTML
                            publishHTML([
                                reportDir: 'bugtracker-backend/reports',
                                reportFiles: 'coverage.html',
                                reportName: 'Backend Coverage Report',
                                keepAll: true,
                                alwaysLinkToLastBuild: true,
                                allowMissing: false
                            ])
                        }
                    }
                }

                stage('Unit Tests - Frontend') {
                    steps {
                        dir('bugtracker-frontend') {
                            // Instala dependencias y ejecuta tests de Node.js en Windows
                            bat 'npm ci'
                            bat 'npm test'

                            // Crear carpeta de reportes si no existe
                            bat 'if not exist reports mkdir reports'
                            bat 'move /Y coverage reports\\'
                        }
                    }
                    post {
                        always {
                            // Publicar reporte HTML
                            publishHTML([
                                reportDir: 'bugtracker-frontend/reports',
                                reportFiles: 'coverage/index.html',
                                reportName: 'Frontend Coverage Report',
                                keepAll: true,
                                alwaysLinkToLastBuild: true,
                                allowMissing: false
                            ])
                        }
                    }
                }

            } // end parallel
        }
    }

    post {
        always {
            // Limpia workspace al final
            cleanWs()
        }
    }
}
