pipeline {
    agent any

    tools {
        go 'Go' //
    }

    stages {
        stage('Unit Tests - Backend') {
            steps {
                dir('bugtracker-backend') {
                    bat 'go version'
                    bat 'go test -v ./...'
                }
            }
        }
    }
}
