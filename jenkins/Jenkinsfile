pipeline {
    agent any

    tools {
        go 'Go'
        nodejs 'NodeJS_LTS'
    }

    stages {
        stage('Unit Tests - Backend') {
            steps {
                dir('bugtracker-backend') {
                    bat 'go version'
                    bat 'go test -v ./...'
                }
            }
        }

        stage('Unit Tests - Frontend') {
            steps {
                dir('bugtracker-frontend') {
                    bat 'node -v'
                    bat 'npm ci'
                    bat 'npm test'
                }
            }
        }
    }
}
