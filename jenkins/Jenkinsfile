pipeline {
    agent any
    tools {
        go 'Go'                // Exact name in Jenkins Global Tool Config
        nodejs 'NodeJS_LTS'    // Exact name in Jenkins Global Tool Config
    }

        stages {
        stage('Execute Unit Tests') {
            parallel {

                stage('Unit Tests - Backend') {
                    steps {
                        dir('bugtracker-backend') {
                            bat 'go version'

                            // Run tests and generate coverage
                            bat 'go test -v ./... > test-results.txt'
                            bat 'go test -coverprofile=coverage.out -covermode=atomic ./...'
                            bat 'go tool cover -html=coverage.out -o coverage.html'

                            // Create reports folder and move coverage.html
                            bat 'if not exist reports mkdir reports'
                            bat 'move /Y coverage.html reports\\'

                            // Convert test-results.txt to JUnit XML using go-junit-report
                            bat 'go-junit-report < test-results.txt > test-results.xml'
                        }
                    }
                    post {
                        always {
                            junit 'bugtracker-backend/test-results.xml'
                            publishHTML([
                                reportDir: 'bugtracker-backend/reports',
                                reportFiles: 'coverage.html',
                                reportName: 'Backend Coverage Report',
                                keepAll: true,
                                alwaysLinkToLastBuild: true,
                                allowMissing: false
                            ])
                        }
                    }
                }

                stage('Unit Tests - Frontend') {
                    steps {
                        dir('bugtracker-frontend') {
                            bat 'node -v'
                            bat 'npm -v'

                            // Install dependencies and run tests
                            bat 'npm ci'
                            bat 'npx playwright test --reporter=html,junit'  // generates coverage & JUnit XML

                            // Create reports folder and move coverage
                            bat 'if not exist reports mkdir reports'
                            bat 'move /Y playwright-report reports\\'
                        }
                    }
                    post {
                        always {
                            junit 'bugtracker-frontend/reports/junit.xml'
                            publishHTML([
                                reportDir: 'bugtracker-frontend/reports',
                                reportFiles: 'index.html',
                                reportName: 'Frontend Coverage Report',
                                keepAll: true,
                                alwaysLinkToLastBuild: true,
                                allowMissing: false
                            ])
                        }
                    }
                }

            } // end parallel
        }

        stage('Launch Applications') {
            steps {
                echo "Launching backend and frontend apps..."
                // Start backend
                bat "start cmd /c \"cd bugtracker-backend && npm start -- --port %BACKEND_PORT%\""
                // Start frontend
                bat "start cmd /c \"cd bugtracker-frontend && npm start -- --port %FRONTEND_PORT%\""
            }
        }

    } // end stages

    post {
        always {
            cleanWs()
        }
    }
}
