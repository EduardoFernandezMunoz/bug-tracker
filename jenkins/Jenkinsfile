pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/EduardoFernandezMunoz/bug-tracker.git'
            }
        }

        stage('Run Backend Tests') {
            steps {
                bat 'go test ./... -v'
            }
        }
    }
}
